{
  "name": "hello",
  "version": "1.0.0",
  "description": "",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "mkdir -p build && cp app/bcx.node.js build/  && tsc",
    "dev": "ts-node-dev --respawn --transpileOnly ./app/main.ts",
    "prod": "cp app/bcx.node.js build/  && tsc && node ./build/main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.0",
    "express": "^4.17.1",
    "ts-node-dev": "^1.0.0-pre.40",
    "typescript": "^3.5.3"
  }
}
